# الدليل العملي — Grading SaaS

يُحدَّث مع كل ميزة. الترتيب ترتيب الأولوية: لا تنتقل للمرحلة التالية قبل إنهاء السابقة.
**الإطلاق (المراحل 1 إلى 4) لا يعتمد على واتساب ولا على Meta إطلاقاً.** واتساب إضافة اختيارية (المرحلة 5).

## المرحلة 1 — أمان وإعدادات لا تحتمل التأجيل

1. **Supabase → Authentication → URL Configuration:** ضع `https://fast-grading.vercel.app` في *Site URL*، وأضف `https://fast-grading.vercel.app/**` و`http://localhost:3000/**` في *Redirect URLs*. (بدونها لا تعمل الدعوات ولا استعادة كلمة المرور.)
2. **المفتاح السري القديم `sb_secret_VQdh…`:** تأكد أنه غير مستخدم في BusPulse أو غيره، ثم احذفه من Supabase → Settings → API Keys.
3. **`Fast Grading Pswds.docx`** (في OneDrive): انقل كلمات المرور إلى مدير كلمات مرور واحذف الملف.
4. **Leaked password protection:** Auth → Providers → Email (قد تتطلب خطة Pro).

## المرحلة 2 — جاهزية الإنتاج (قبل أول مدرسة حقيقية)

1. **خطة Supabase Pro** للمشروع: المشاريع المجانية تُوقَف عند عدم الاستعمال (حصل مع مشروعك سابقاً) ولا تملك نسخاً احتياطية يومية. أدخلت بيانات حقيقية = لازم Pro.
2. **بريد الدعوات (SMTP مخصص):** خدمة البريد الافتراضية في Supabase للتجربة فقط (عدد ضئيل بالساعة، وترسل لفريق المشروع فقط). قبل دعوة أول أستاذ اربط مزوّد بريد (Resend أو Brevo أو غيرهما) من Auth → SMTP Settings.
3. **صفحة خصوصية وشروط استخدام:** أُنشئتا (`/privacy` و`/terms`) وتظهر روابطهما في صفحة الدخول. هما **نموذج مبدئي عام** كتبته أنا، وتحتاجان مراجعة مستشار قانوني في بلدكم قبل أول مدرسة (خصوصاً بيانات القاصرين ومدة الاحتفاظ). أضف بريد التواصل بمتغير النشر `NEXT_PUBLIC_CONTACT_EMAIL` في Vercel. وموافقة الأهل على تسجيل بيانات أبنائهم مسؤولية المدرسة (نموذج تسجيل أو بند في العقد).
4. **نطاق خاص** (اختياري): ربط دومين المدرسة بـVercel.
5. **إنشاء مدرسة جديدة:** من **لوحة المنصة** (`/platform`، تظهر في القائمة لك فقط): اكتب اسم المدرسة واسم مديرها وبريده فتُنشأ المدرسة ويصله بريد دعوة، وتفعّل لها الإضافات من الجدول نفسه. (يتطلب SMTP مخصصاً من البند 2.)
6. **بعد أول دخول لمدير المدرسة:** يفتح **معلومات المدرسة** ويكمل: الاسم، المدير، الهاتف، العنوان، **رمز الدولة**، تذييل الشهادة (هذه تظهر على الشهادات والرسائل)، ثم يتبع «قائمة تهيئة مدرسة جديدة» في الصفحة نفسها.

## المرحلة 3 — الاختبار الأول على الموقع (ساعة)

سجّل الدخول من `fast-grading.vercel.app` وأدخل بيانات تجريبية بالترتيب (كل شاشة تعتمد على ما قبلها):

1. **المواد** ← 2. **المراحل** ← 3. **قوالب الاختبارات** ← 4. **الهيكل الأكاديمي** (سنة، فصول، بنود وأوزان) ← 5. **سياسة العلامات** (حد النجاح) ← 6. **الصفوف** ← 7. **الطلاب** ← 8. **الامتحانات** (اربط كل امتحان ببند) ← 9. **إدخال العلامات** ← 10. **التقارير** (معدلات الصف، الشهادة النهائية).

سجّل كل خلل (لقطة شاشة + ما توقعته) وأرسله لي دفعة واحدة، وأصلحه قبل المرحلة 4.

## المرحلة 4 — التشغيل الفعلي

1. **الاستيراد الجماعي** للطلاب من CSV.
2. **الهيكل التنظيمي والمستخدمون:** المباني والطوابق، ثم دعوة الناظرين والأساتذة، ثم إسناد الأساتذة للشعب والمواد.
3. **إدخال العلامات** يدوياً أو **صوتياً** (Chrome/Edge فقط؛ راجع المسوّدة قبل «تطبيق» ثم «حفظ العلامات»).
4. **التقارير والشهادات** كل فصل، و**الامتحان التكميلي** آخر السنة.

## المرحلة 5 — الإضافات الاختيارية (تُباع أو تُفعَّل عند الطلب)

**الاشتراك** تفعّله أنت لكل مدرسة على حدة (لا تستطيع المدرسة تفعيله). ما لم يُفعَّل لا تظهر شاشة الخدمة ولا يعمل شيء منها، لا في الواجهة ولا في قاعدة البيانات، ولا تؤثر على بقية البرنامج. وبعد الاشتراك **للمدرسة أن تشغّل الخدمة أو توقفها** من «إعدادات واتساب»، وتُدخل هي بيانات رقمها وحسابها لدى Meta (مخزّنة مشفّرة، لا ترى المنصة أسرارها) وتكتب صياغة رسائلها. رقم واتساب لكل مدرسة وليس على المنصة.

| الإضافة | المفتاح | الحالة |
|---|---|---|
| إشعارات أولياء الأمور عبر واتساب | `whatsapp_notifications` | جاهزة تقنياً؛ تحتاج مستلزمات Meta (راجع [WHATSAPP_SETUP_AR.md](WHATSAPP_SETUP_AR.md)) |

**تفعيل إضافة لمدرسة** (من Supabase → SQL Editor، أو اطلب مني):
```sql
insert into account_features (account_id, feature_key, note)
values ('<account-id>', 'whatsapp_notifications', 'تفعيل بتاريخ ...')
on conflict (account_id, feature_key) do update set enabled = true;
```
**إيقافها:** `update account_features set enabled = false where account_id = '<account-id>' and feature_key = 'whatsapp_notifications';`

**سير الرسائل داخل المدرسة (عند تفعيل الإضافة):**
1. مدير المدرسة يكتب صياغة كل نوع مراسلة من **قوالب الرسائل** (نتيجة امتحان، دون حد النجاح، نتيجة الفصل، دعوة تكميلي، نتيجة الترفيع) وتبقى الافتراضية إن لم يعدّل.
2. المُعِدّ ينشئ **مسودة** من **مراسلات أولياء الأمور**؛ تُحدَّد كل الرسائل افتراضياً، ويُزيل من لا يريد ويُعدّل أي نص استثنائياً.
3. **الاعتماد إلزامي قبل الإرسال** (يُفرض في قاعدة البيانات): يعتمد المُعِدّ بنفسه أو شخص آخر بصلاحية «اعتماد الرسائل» حسب خيار المدرسة في «معلومات المدرسة».
4. بعد الاعتماد: إرسال تلقائي للرسائل المؤهلة (قالب معتمد من واتساب وغير معدّلة)، وفتح واتساب يدوياً للباقي (المعدّلة استثنائياً أو بلا قالب معتمد). النص المعتمد يتجمّد.

**قبل تفعيل الاشتراك لمدرسة:** الاتفاق التجاري بينكما (رسوم الخدمة)، وأنها ستجهّز مستلزمات Meta على حسابها (القسم «ما تحتاجه المدرسة من Meta» في الملف المذكور)، وموافقة أولياء الأمور. كلفة الرسائل نفسها على حساب المدرسة لدى Meta.

## بعدها: BusPulse

عناصره المفتوحة في ذاكرة المشروع: اختبار تسجيل الدخول الفعلي لكل الأدوار، مراجعة `wa-webhook` بعد الترميم، وتوقيت تذكير الدفع (UTC بدل بيروت).

## تحقّق من نطاق التقارير (2026-09-22)

تقارير العلامات (بطاقة العلامات، معدلات الصف، الشهادات، النجاح/الرسوب، نشاط الإدخال) مقيّدة فعلاً في قاعدة البيانات: كل مستخدم يرى فقط ما يخص صفوفه/مواده المُسنَدة إليه أو ما مُنح له صراحة. بيانات الجدول الأساسي (الطلاب، الصفوف، التسجيلات، إسناد الأساتذة) تبقى مرئية لأي مستخدم في المدرسة نفسها (بنية قديمة يعتمد عليها عرض الأسماء في شاشات كثيرة). تقريرا «المستخدمون» و«سجل التدقيق» كانا يعتمدان على إخفاء الواجهة فقط؛ أُغلقا الآن في قاعدة البيانات نفسها (ترحيل 47): سجل التدقيق يتطلب config.manage أو users.manage أو notifications.approve، وقراءة صلاحيات مستخدم آخر تتطلب users.manage.
