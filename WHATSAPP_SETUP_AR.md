# تفعيل الإرسال التلقائي عبر واتساب (Cloud API)

الكود جاهز ومنشور (Edge Functions: `send-whatsapp` و`whatsapp-webhook`، والملفان `35_whatsapp_delivery.sql` و`36_account_features.sql`). ما ينقص هو حسابك عند Meta وإضافة المفاتيح.

## هذه إضافة اختيارية لا تمس البنية الأساسية

- شاشة «إشعارات أولياء الأمور» **مخفية** ولا يعمل شيء منها لأي مدرسة حتى تفعّلها أنت لها بالمفتاح `whatsapp_notifications` (انظر الخطوة 10).
- الإنفاذ في قاعدة البيانات (سياسات RLS) وفي الدالة `send-whatsapp`، فلا يمكن لمدرسة تجاوزها بتعديل الواجهة، ولا تستطيع تفعيلها بنفسها.
- إيقافها لاحقاً بتحديث `enabled = false` دون حذف أي بيانات أو المساس ببقية البرنامج.
- إلى أن تكتمل مستلزمات Meta تعمل الإضافة (إن فُعّلت) بأسلوب «فتح واتساب برسالة جاهزة» بلا أي حساب مزوّد.

**قيد حالي:** المفاتيح في Secrets مشتركة على مستوى المشروع، أي رقم إرسال واحد لكل المدارس المفعَّلة (اسم المدرسة يظهر داخل نص الرسالة). إن أردت أن ترسل كل مدرسة من رقمها الخاص فهذا يحتاج تطويراً إضافياً (تخزين بيانات اعتماد لكل حساب في Supabase Vault)؛ قرّر ذلك قبل البيع لأكثر من مدرسة.

## صياغة كل مدرسة ومراجعة الرسائل

- لكل مدرسة صياغتها الخاصة لكل نوع مراسلة، يعدّلها مدير المدرسة من **قوالب الرسائل**. النصان في آخر هذا الملف مجرد صياغة افتراضية.
- **كل تعديل على النص يبطل اعتماد واتساب السابق** ويلزم تقديمه من جديد لـMeta (تعرض الشاشة النص الجاهز للتقديم بصيغة `{{1}}`). إلى أن يُعتمد يُرسل هذا النوع بفتح واتساب يدوياً.
- عند مشاركة رقم واحد بين مدارس عدة، أعطِ كل مدرسة **اسم قالب مختلفاً** (مثلاً `grade_result_alnoor`) من إعدادات واتساب في بطاقة القالب.
- لا رسالة تُرسل قبل مراجعتها واعتمادها؛ والرسالة التي يُعدَّل نصها استثنائياً عند المراجعة تُرسل يدوياً لأنها تخرج عن القالب المعتمد.

## الخطوات بالترتيب

1. **حساب Meta Business** باسم المدرسة: business.facebook.com.
2. **رقم هاتف مخصص** للإرسال، غير مسجّل على تطبيق واتساب العادي (أو احذف تسجيله أولاً).
3. **Meta for Developers** (developers.facebook.com) ← Create App (نوع Business) ← أضف منتج **WhatsApp** ← اربطه بحساب الأعمال وسجّل الرقم.
4. **قدّم القالبين بالأسفل للاعتماد** (WhatsApp Manager ← Message templates): الفئة **Utility**، اللغة **Arabic (ar)**، والاسم كما هو حرفياً. الاعتماد يستغرق من دقائق إلى أيام.
5. **System User دائم:** Business Settings ← Users ← System users ← أنشئ مستخدماً ← أسند له تطبيقك ← أنشئ Token بصلاحيتي `whatsapp_business_messaging` و`whatsapp_business_management` (بلا تاريخ انتهاء).
6. **جمع القيم:** `Phone Number ID` (من WhatsApp ← API Setup)، و`App Secret` (App settings ← Basic).
7. **ضع المفاتيح في Supabase** (مشروع Fast Grading) ← Edge Functions ← Secrets — أنت بنفسك، لا تلصقها في أي محادثة:

| الاسم | القيمة |
|---|---|
| `WHATSAPP_TOKEN` | توكن الـSystem User |
| `WHATSAPP_PHONE_ID` | Phone Number ID |
| `WHATSAPP_APP_SECRET` | App Secret (للتحقق من توقيع حالات التسليم) |
| `WHATSAPP_VERIFY_TOKEN` | أي نص عشوائي طويل تختاره أنت |

8. **Webhook (لمعرفة التسليم/القراءة):** في Meta ← WhatsApp ← Configuration ← Webhook:
   - Callback URL: `https://rpxfbbznsctoyevfcvyu.supabase.co/functions/v1/whatsapp-webhook`
   - Verify token: نفس قيمة `WHATSAPP_VERIFY_TOKEN`
   - اشترك في الحقل **messages** (منه تصل حالات الرسائل).
9. **موافقة أولياء الأمور** على الاستلام (نموذج تسجيل أو بند في عقد التسجيل).
10. **فعّل الإضافة للحساب** (SQL Editor في Supabase):
    ```sql
    insert into account_features (account_id, feature_key, note)
    values ('<account-id>', 'whatsapp_notifications', 'تفعيل تجريبي')
    on conflict (account_id, feature_key) do update set enabled = true;
    ```
11. **ابدأ بتجربة** على رقمك: ضع رقمك رقمَ ولي أمر لطالب تجريبي، اختر قالب «نتيجة امتحان» وأرسل. ستظهر الحالة: قُبلت ← سُلّمت ← قُرئت.

## القالب 1 — الاسم: `grade_result_ar` (Utility، ar)

```
السلام عليكم {{1}}،
نفيدكم بأن علامة الطالب/ة {{2}} في مادة {{3}} ({{4}}) هي {{5}} من {{6}}.
مع تحيات إدارة {{7}}، شكراً لثقتكم.
```
أمثلة المتغيرات عند التقديم: 1 = أم أحمد، 2 = أحمد خليل، 3 = الرياضيات، 4 = اختبار الفصل الأول، 5 = 85، 6 = 100، 7 = مدرسة النور.

## القالب 2 — الاسم: `grade_low_ar` (Utility، ar)

```
السلام عليكم {{1}}،
نودّ إعلامكم أن علامة الطالب/ة {{2}} في مادة {{3}} ({{4}}) هي {{5}} من {{6}}، وهي دون حد النجاح. نرجو التواصل مع إدارة {{7}} لمتابعة الأمر.
```

قواعد واتساب: لا يبدأ القالب ولا ينتهي بمتغير، ولا يكون فيه متغيران متجاوران بلا نص بينهما، والمتغير لا يحوي أسطراً جديدة.

## كيف يعمل داخلياً

- زر «إرسال» يسجّل كل رسالة في `notifications_log` (حالة pending + القالب والمتغيرات)، ثم يستدعي `send-whatsapp` بجلسة المستخدم.
- الدالة تتحقق من صلاحية `notifications.send`، وتحجز الصفوف ذرّياً (`claimed_at`) فلا تُرسل الرسالة مرتين، وترسل 5 في الوقت نفسه، وتسجّل رقم رسالة واتساب أو نص الخطأ.
- `whatsapp-webhook` يستقبل حالات التسليم ويحدّث السجل، وهو يرفض أي طلب بلا توقيع Meta صالح.
- قبل وضع المفاتيح تُظهر الشاشة «الإرسال التلقائي غير مفعّل» وتستعمل أسلوب فتح واتساب.
- رسائل النص الحر خارج نافذة 24 ساعة يرفضها واتساب؛ لذلك الإرسال التلقائي يقتصر على القوالب المعتمدة.

## استكشاف الأخطاء

| الظهور في عمود التسليم | السبب المرجّح |
|---|---|
| Template name does not exist | القالب غير معتمد بعد، أو الاسم/اللغة غير مطابقين |
| Parameter count mismatch | عدد المتغيرات بالقالب المعتمد ≠ 7 |
| Recipient not in allowed list | الحساب ما زال بوضع الاختبار: أضف الرقم لقائمة المستقبلين المسموحين أو فعّل الحساب |
| (#131030) / رقم غير صالح | رقم ولي الأمر غير صحيح أو ليس على واتساب |
| Re-engagement message | إرسال نص حر خارج 24 ساعة: استعمل قالباً |
