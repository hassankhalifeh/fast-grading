# خدمة واتساب لأولياء الأمور (إضافة اختيارية لكل مدرسة)

الكود جاهز ومنشور. تعمل الخدمة **لكل مدرسة على حدة**، برقمها وحسابها لدى Meta، وعلى مسؤوليتها وكلفتها. إدارة المنصة تفعّل الاشتراك فقط.

## من يفعل ماذا

| الجهة | ما تفعله |
|---|---|
| **إدارة المنصة** | تفعّل الاشتراك للمدرسة من `/platform` (خانة «مراسلات واتساب») أو توقفه. هذا كل ما يلزم منها. |
| **إدارة المدرسة** | تُشغّل/توقف الخدمة من **إعدادات واتساب**، وتُدخل بيانات رقمها، وتكتب صياغة رسائلها، وتقدّم قوالبها لـMeta، وتراجع وتعتمد رسائلها. |

الحالات:
- **غير مشترك:** لا تظهر أي شاشة للخدمة، ولا يعمل شيء منها في قاعدة البيانات.
- **مشترك وموقوف من المدرسة:** تختفي شاشات المراسلات والقوالب (يبقى «إعدادات واتساب» لإعادة التشغيل)، ولا يمكن الإرسال.
- **مشترك ومشغّل بلا بيانات اتصال:** تعمل المراسلات بأسلوب «فتح واتساب برسالة جاهزة» (يضغط المرسل «إرسال» داخل واتساب).
- **مشترك ومشغّل ومتصل + قالب معتمد من Meta:** إرسال تلقائي من رقم المدرسة، مع تتبع التسليم والقراءة.

## ما تحتاجه المدرسة من Meta (بالترتيب)

1. حساب **Meta Business** باسم المدرسة: business.facebook.com.
2. **رقم هاتف مخصص** للمدرسة غير مسجّل على تطبيق واتساب العادي (أو يُحذف تسجيله أولاً).
3. **Meta for Developers** ← Create App (نوع Business) ← منتج **WhatsApp** ← ربطه بحساب الأعمال وتسجيل الرقم.
4. **System User دائم:** Business Settings ← Users ← System users ← تطبيق المدرسة ← Token بصلاحيتي `whatsapp_business_messaging` و`whatsapp_business_management` (بلا تاريخ انتهاء).
5. جمع: **Phone Number ID** و**WABA ID** (من WhatsApp ← API Setup) و**App Secret** (App settings ← Basic).
6. **موافقة أولياء الأمور** على الاستلام (نموذج تسجيل أو بند في عقد التسجيل).
7. بطاقة دفع في Meta؛ الكلفة على حساب المدرسة حسب الدولة والفئة.

## ما تفعله المدرسة داخل البرنامج

1. **إعدادات واتساب:** تدخل Phone Number ID وWABA ID والتوكن وApp Secret، وتضغط «حفظ وفحص الاتصال». تُفحص البيانات لدى Meta قبل الحفظ، وتُخزَّن الأسرار **مشفّرة في الخادم ولا تظهر بعد ذلك لأحد**، حتى إدارة المنصة لا ترى التوكن.
2. **تتبع التسليم (اختياري):** تنسخ من الصفحة نفسها عنوان الـWebhook ورمز التحقق إلى تطبيقها لدى Meta وتشترك في الحقل **messages**. عنوان كل مدرسة خاص بها، ولا تتأثر رسائل مدرسة أخرى.
3. **قوالب الرسائل:** تكتب صياغتها لكل نوع مراسلة. كل تعديل على النص يبطل اعتماد Meta السابق، وتعرض الشاشة النص الجاهز للتقديم بصيغة `{{1}}` (الفئة **Utility**، اللغة **العربية**). تكتب اسم القالب وتختار «معتمد» بعد أن تعتمده Meta؛ ويجب أن يكون الاسم فريداً في حساب المدرسة.
4. **مراسلات أولياء الأمور:** تُنشأ مسودة ← تُراجع وتُعدَّل استثنائياً ← تُعتمد (إلزامي) ← تُرسل. المعدّلة استثنائياً تُرسل يدوياً لأنها تخرج عن القالب المعتمد.

## قواعد واتساب للقوالب

لا يبدأ القالب ولا ينتهي بمتغير، ولا يكون فيه متغيران متجاوران بلا نص بينهما، والمتغير لا يحوي أسطراً جديدة. الشاشة تحذّر من هذا أثناء الكتابة.

## نصان افتراضيان (للاسترشاد)

```
السلام عليكم {{1}}،
نفيدكم بأن علامة الطالب/ة {{2}} في مادة {{3}} ({{4}}) هي {{5}} من {{6}}.
مع تحيات إدارة {{7}}، شكراً لثقتكم.
```
```
السلام عليكم {{1}}،
نودّ إعلامكم أن علامة الطالب/ة {{2}} في مادة {{3}} ({{4}}) هي {{5}} من {{6}}، وهي دون حد النجاح. نرجو التواصل مع إدارة {{7}} لمتابعة الأمر.
```

## للمالك: تفعيل يدوي بـSQL (بديل عن `/platform`)

```sql
insert into account_features (account_id, feature_key, note)
values ('<account-id>', 'whatsapp_notifications', 'اشتراك')
on conflict (account_id, feature_key) do update set enabled = true;
```
الإيقاف: `update account_features set enabled = false where account_id = '<account-id>' and feature_key = 'whatsapp_notifications';`

## كيف يعمل داخلياً

- الاشتراك: `account_features`. إيقاف المدرسة: `account_whatsapp_config.enabled`. الاثنان معاً = `whatsapp_active()`، وهو شرط سياسات RLS على القوالب والدفعات والرسائل وشرط دالة الإرسال.
- الأسرار في Supabase Vault؛ الدوال `wa_*` لا تعمل إلا بمفتاح الخدمة (Edge Functions). المتصفح لا يقرأ الجدول ولا الأسرار.
- `whatsapp-admin`: يفحص البيانات لدى Meta ثم يحفظها. `send-whatsapp`: يرسل من رقم المدرسة، ويتطلب دفعة معتمدة ويحجز كل رسالة مرة واحدة. `whatsapp-webhook?a=<account>`: يحدّث حالة التسليم لتلك المدرسة فقط بعد التحقق من رمز المدرسة وتوقيعها.

## استكشاف الأخطاء

| الظهور | السبب المرجّح |
|---|---|
| Template name does not exist | القالب غير معتمد بعد، أو الاسم/اللغة غير مطابقين |
| Parameter count mismatch | عدد المتغيرات في القالب المعتمد يختلف عن نص القالب في البرنامج |
| Recipient not in allowed list | حساب Meta ما زال بوضع الاختبار: تُضاف الأرقام لقائمة المسموحين أو يُفعَّل الحساب |
| (#131030) / رقم غير صالح | رقم ولي الأمر غير صحيح أو ليس على واتساب |
| Error validating access token | التوكن منتهٍ أو بلا الصلاحيات المطلوبة: أدخل توكناً جديداً |
